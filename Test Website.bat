rem Requires Gulp to be installed 
rem To install Gulp from Node Package Manager, run the following from the command line: npm install -g gulp-cli
rem Then just run this batch file to compile the site. It will automatcally open once it finishes compiling. 
gulp